/**
 * 1. create firebase project;
 * 2. create firebase app;
 * 3. npm i firebase;
 * 4. save firebase config and export default app;
 * 5. Build > Authentications > get started > enable sign in;
 * 6. create sign up and login route
 **/ 

/**
 * 1. create a context(API) provider file;
 * 2. create a context and set provider;
 * 3. set the children props;
 * 4. set context value;
 * 5. set the AuthProvider to main router Provider;
 * 
 * */ 

/**
 * HOSTING:
 * --------------------------
 * One time per PC
 * 1. you need to install: npm install -g firebase-tools
 * 2. firebase login for one time per pc
 * --------------------------
 * For each project: one time
 * 1. firebase in it
 * 2. proceed
 * 3. firebase hosting: (up/down arrow to change and space button for select)
 * 4. existing project
 * 5. select the project carefully.
 * 6. which project as public directory: dist(vite)
 * 7. single page application: yes
 * 8. continues deployment: no
 * 
 * For every time deploy:
 * 1. npm run build
 * 2. firebase deploy
 **/ 